import React from "react";

const event  = ({name,date,location}) => {
  return (
    <div>
        <p>{name}</p>
        <p>{date}</p>
        <p>{location}</p>
    <Abc/>
    </div>
  )
}

export default event
